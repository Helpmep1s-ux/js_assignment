const names = ['Alan','Bron','Chad','David','Ezekiel'];

//forEach does the same function for all elements of the array

names.forEach( (element , index)=> {                    //element and index are parameters to the arrow function
    console.log(element , index);
});